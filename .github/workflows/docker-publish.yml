name: Docker Image Publish

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: Release tag to publish (optional)
        required: false
        type: string

permissions:
  contents: read
  packages: write

jobs:
  docker-publish:
    uses: matt-riley/matt-riley-ci/.github/workflows/docker-ghcr-publish.yml@071c2c2bbe410f73126d3cefd0a0b6812b3565f6 # v2
    with:
      tag-name: ${{ github.event_name == 'release' && github.event.release.tag_name || github.event.inputs.tag_name || '' }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
