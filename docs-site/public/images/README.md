# Visual Assets

This directory contains visual assets for documentation, including:

- **tui/** - Terminal UI (TUI) screenshots and demo GIFs
- **dashboard/** - Web dashboard screenshots (future)

## Guidelines

- Use descriptive filenames
- Compress assets before committing (target: <1MB per file)
- Use descriptive alt text when embedding in documentation
- Prefer relative paths in markdown
